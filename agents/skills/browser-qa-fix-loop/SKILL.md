---
name: browser-qa-fix-loop
description: フロントエンド変更後にブラウザで動作確認し、UI や実行時の問題を見つけて修正し、再度ブラウザで確認する。ユーザーがブラウザ確認、画面の動作検証、UI バグの再現、ブラウザで見つかった問題の修正、修正後の再確認を依頼したときに使用する。
---

# Browser QA Fix Loop

## 目的

フロントエンドの変更や UI バグ調査で、ブラウザ確認、問題の特定、修正、再確認を一連のループとして実行する。

## 起動条件

ユーザーが次のような依頼をしたときに使う。

- ブラウザで動作確認してほしい
- 画面で確認して問題があれば直してほしい
- UI バグを再現して修正してほしい
- 修正後に再度ブラウザで確認してほしい
- フロントエンド変更後の画面確認まで任せたい

## ブラウザの選び方

Cursor で実行している場合は、Cursor 内蔵ブラウザ（`cursor-ide-browser`）を優先して使う。外部ブラウザや別の自動化手段に切り替えるのは、Cursor 内蔵ブラウザで確認できない理由がある場合だけにする。

ブラウザ MCP ツールを呼ぶ前に、必ず対象サーバーの tool descriptor / schema を確認する。既存タブがある場合はタブ一覧を確認し、必要に応じてブラウザを lock してから操作する。

## ワークフロー

1. 目的画面、確認したい挙動、成功条件、対象 URL を整理する。
2. 既存の dev server や Storybook が動いているか確認し、無ければプロジェクトの標準コマンドで起動する。
3. Cursor 内蔵ブラウザで対象画面を開く。
4. `snapshot` を使ってページ構造を確認し、必要に応じて `screenshot`、console、network の情報も見る。
5. 期待と違う挙動、表示崩れ、console error、network error、操作不能な UI を問題として記録する。
6. 問題があれば、関連するコードを最小範囲で調査して原因を特定する。
7. コードを修正する。既存の設計、コンポーネント、テスト、Storybook の書き方に合わせる。
8. リポジトリに合う軽量な検証を実行する。例: lint、typecheck、unit test、Storybook の関連確認。
9. 同じ対象 URL と同じ確認手順でブラウザ再確認を行う。
10. 修正前の問題、修正内容、再確認結果、実行した検証を簡潔に報告する。

## 操作ルール

- クリック、入力、遷移、待機、スクロールなどでページ状態が変わったら、次の構造的操作の前に新しい `snapshot` を取る。
- `snapshot` の ref を優先して操作する。座標クリックが必要な場合は、直前に新しい screenshot を取ってから使う。
- 同じ失敗操作を根拠なく繰り返さない。新しい観測結果、別の ref、状態変化、別仮説がある場合だけ再試行する。
- ログイン、権限、CAPTCHA、手動認証、破壊的確認、必要なデータ不足に当たったら停止して報告する。
- 4 回程度試して進展が無い場合は、現在のページ、試したこと、観測した問題、次に必要な対応を報告する。

## 修正時の注意

- ブラウザで観測した事実を根拠にして修正する。
- 変更範囲は問題解決に必要な最小限にする。
- 既存のユーザー変更を戻さない。
- フォーマット、命名、状態管理、API 呼び出し、テストの置き方は周辺コードに合わせる。
- 修正後は、最初に問題を見つけた操作をもう一度実行して確認する。

## 最終報告

完了時は次を短くまとめる。

- 確認した URL または画面
- 見つけた問題
- 修正内容
- 再確認結果
- 実行した検証コマンド
- 未確認事項や手動対応が必要な点
